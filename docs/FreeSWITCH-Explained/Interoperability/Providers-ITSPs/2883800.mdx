# 服务提供商 Nextiva (美国)

 

## [Nextiva.com](http://Nextiva.com)

最后更新日期：2013年6月11日

Nextiva不使用您的DID作为用户名，我们会生成一个唯一值供您使用。如果您不知道它，请登录[Office Manager](https://manage.nextiva.com/)门户并编辑您的账户，您可以在那里获取您的凭据。

```xml
<include>
  <gateway name="nextiva">
    <!-- 您的用户名，不是您的DID -->
    <param name="username" value="USERNAME"/>
    <param name="password" value="PASSWWORD"/>
    <param name="register" value="true"/>
    <param name="realm" value="sbc.voipdnsservers.com"/>
    <param name="proxy" value="sbc.voipdnsservers.com"/>
    <param name="extension" value="auto_to_user"/>
  </gateway>
</include>
```

## 故障排除 

在配置过程中，您可能需要将以下参数添加到网关设置中，以便成功注册并处理呼叫

```xml
    <param name="from-domain" value="sbc.voipdnsservers.com"/> 
```

如果在远程端的IVR中无法处理DTMF数字，将其添加到conf/sip\_profiles/external.xml文件中

```xml
    <param name="dtmf-duration" value="3000"/> 
```

如果FreeSWITCH从远程端接收到双重数字，请设置您的电话将DTMF作为SIP INFO发送，并在conf/sip\_profiles/internal.xml文件中进行以下调整

```xml
    <param name="liberal-dtmf" value="true"/>
    <param name="dtmf-type" value="info"/> 
```