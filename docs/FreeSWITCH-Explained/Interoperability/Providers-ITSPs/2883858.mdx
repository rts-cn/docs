# 供应商VOXOX (美国)

## [www.voxox.com](http://www.voxox.com)

最后更新日期：2011年5月15日 - 注意：这以前是Telcntris，已经合并到VOXOX中，所以信息可能不准确。

* 您将需要您的Telcentris用户名（可能是可选的？），IP地址。

conf/sip_profiles/external/Telcentris.xml

```xml
 <include>
     <gateway name="Telcentris">
        <param name="username" value="<username>"/>
        <param name="password" value="register:false"/>
        <param name="proxy" value="<IP address>"/>
        <param name="expire-seconds" value="800"/>
        <param name="register" value="false"/>
        <param name="context" value="<your context>"/>

    </gateway>
</include>
```