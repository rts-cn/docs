# NetBSD

 

维护者需求

我们需要您来维护这个 FreeSWITCH 的端口。请在 Freenode IRC 频道 #freeswitch 或 [freeswitch-users@lists.freeswitch.org](mailto:freeswitch-users@lists.freeswitch.org) 邮件列表上联系其中一位 FreeSWITCH 核心开发者，以帮助他们使代码保持最新的操作系统。

* 1 [介绍](#introduction)
* 2 [安装](#installation)

## 介绍

这是[安装](../index.mdx#about)文档的一部分，您应该首先查阅。它提供了适用于 NetBSD 的特殊说明。在 NetBSD 6.1.4 (amd64) 上经过测试。

## 安装

```bash
export PKG_PATH=http://ftp.netbsd.org/pub/pkgsrc/packages/NetBSD/`uname -m`/`uname -r`/All/
pkg_add -v git automake autoconf libtool gmake jpeg pkg-config pcre speex openssl ncurses sqlite3 ldns
mkdir ~/src
cd ~/src
git clone https://stash.freeswitch.org/scm/fs/freeswitch.git
cd freeswitch.git
./bootstrap.sh -j
./configure -C
gmake
gmake install cd-sounds-install cd-moh-install
```