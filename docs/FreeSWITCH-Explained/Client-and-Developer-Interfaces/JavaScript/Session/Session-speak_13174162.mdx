# 会话讲述

## 关于

使用文本到语音引擎说出一个短语。你必须加载一个文本到语音模块才能使其工作。

点击这里展开目录

* 1 [概述](#概述)
   * 1.1 [示例](#示例)
      * 1.1.1 [带回调函数](#带回调函数)
         * 1.1.2 [读取文件](#读取文件)

## 概述

speak(engine,voice,phrase,timer)

* engine - 使用的引擎
* voice - 使用的语音名称
* phrase - 希望它说的内容
* timer - 可选计时器 (例如 "soft")

在 cepstral 中，短语中可以包含 SSML 标签。请参见 <http://www.cepstral.com/cgi-bin/support?page=faq&type=ssml> 了解更多信息

在 cepstral 中，如果短语以 / 开头，它将打开指定的文件并读取其内容，而不是字符串 - 需要特定于 Windows 的路径信息

### 示例

```xml
session.speak("cepstral","David","Hello from FreeSwitch");
```

#### 带回调函数

```xml
function my_dtmf_callback(dtmf_str, digits, args)
{
    console_log("debug", "you dialed the following " + dtmf_str + " + digits + "\n");
    return true;
}

session.speak("cepstral", "david", '<break time="500ms"/>Hello this is a test of the break tag', my_dtmf_callback);
```

请确保不要将回调函数的名称放在引号中。

#### 读取文件

```xml
session.speak("cepstral","David","/etc/passwd");
```