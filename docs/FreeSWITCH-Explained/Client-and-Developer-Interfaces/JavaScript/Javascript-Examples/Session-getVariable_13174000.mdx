# 获取会话变量

单击此处展开目录

* 1 [概述](#概述)
* 2 [示例](#示例)

在指定的通道上获取变量。

要获取可以使用的变量名称列表，可以使用info应用程序显示它：

```xml
<action application="info" />
```

## 概述

```xml
getVariable(variable)
```

* variable - 包含变量名称的字符串

## 示例

```xml
Lua:
local caller_id = session:getVariable("caller_id_number")
```

```xml
JavaScript:
var caller_id = session.getVariable("caller_id_number")
```

另请参阅

* [Javascript](https://wiki.freeswitch.org/wiki/Javascript)