# mod_ldap

## 关于

mod_ldap通过目录层提供对LDAP的访问。它可以用于目录布局，并且没有任何全局变量。它使用简单的未加密身份验证和已弃用的功能。所以我们需要做的就是开始重新编写。

点击此处展开目录

* [1 新建模块](#新建模块)
* [2 旧模块示例（mod_ldap）](#旧模块示例mod_ldap)

## 新建模块

* 可以访问SASL和简单认证。
* 连接时请使用安全连接，请不要支持未加密连接。
* 使用目录接口。

## 旧模块示例（mod_ldap）

您可以将其用作拨号计划。conf/autoload_configs/dialplan_directory.conf.xml：

```xml
<configuration name="dialplan_directory.conf" description="Dialplan Directory">
  <settings>
    <param name="directory-name" value="ldap"/>
    <param name="host" value="ldap.example.org"/>
    <param name="dn" value="uid=sample,ou=freeswitch,dc=example,dc=org"/>
    <param name="pass" value="c-base"/>
    <param name="base" value="ou=freeswitch,dc=example,dc=org"/>
  </settings>
</configuration> 
```

您的LDAP应该有标有“exten”、“callflow”和“context”的字段。

svn r16506 --[Lynxis](https://wiki.freeswitch.org/index.php?title=User:Lynxis&action=edit&redlink=1 "User:Lynxis (page does not exist)") 02:04, 2010年1月25日 (UTC)