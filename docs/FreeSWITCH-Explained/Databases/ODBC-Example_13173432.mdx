# ODBC示例

## 关于

此页面包含一个更好的ODBC使用示例

单击这里展开目录

* 1 [JavaScript ODBC示例](#javascript-odbc-example)
* 2 [故障排除](#troubleshooting)
* 3 [参见](#see-also)

## JavaScript ODBC示例

```xml
use("ODBC");

var db = new ODBC("dsnName", "username", "password");
var sql = "select * from users";

db.connect();

if (!db.query(sql))
  session.hangup(); //可能想要说些什么好的。

while (db.nextRow())
{
  row = db.getData();
  console_log("debug", "UserName: " + row["user_name"]);
}
```

## 故障排除

如果您看到如下错误信息：

```xml
[ERR] mod_spidermonkey.c:2697 js_api_use() Error loading ODBC
[ERR] test-odbc.js:3 mod_spidermonkey()  ReferenceError: ODBC is not defined
```

请确保 _mod\_spidermonkey\_odbc_ 模块已安装，并在启动时成功加载。它(以及所有其他spidermonkey扩展) 都配置为从 _spidermonkey.conf.xml_ 中加载。

## 参见

* [ODBC](./ODBC_13173465.mdx#odbc)