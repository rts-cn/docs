# 用Freeswitch拨打电话链接

 

单击此处展开目录

* 1 [通用](#-freeswitch--用freeswitch拨打电话链接)
* 2 [Firefox](#-freeswitch--用freeswitch拨打电话链接)
* 3 [Thunderbird](#-freeswitch--用freeswitch拨打电话链接)
* 4 [测试](#-freeswitch--用freeswitch拨打电话链接)

## 通用

在您的拨号计划中为电话链接编写处理程序。

```xml
 <include>
   <extension name="handle_tel">
     <condition field="destination_number" expression="^tel:(.*)$">
       <action application="transfer" data="$1 XML default"/>
     </condition>
   </extension>
 </include>
```

##   
Firefox

进入about:config页面，添加以下新的布尔属性。

```xml
network.protocol-handler.external.tel         true
network.protocol-handler.expose.tel           false
network.protocol-handler.warn-external.tel    false
```

打开您喜欢的文本编辑器，保存以下平台的脚本。

Windows - 以.bat为扩展名。

```xml
c:\progra~1\freeswitch\fs_cli.exe -x "pa call %1"
```

Linux - 您的用户可以执行。

```xml
/usr/local/freeswitch/bin/fs_cli -x "pa call $1"
```

在任何页面上单击任何电话链接，Firefox应该提示您使用一个应用程序打开它...输入脚本的路径，一切都应该正常。

## Thunderbird

## 测试

```xml
[tel:888*543 FreeSWITCH Conference via ISN]
[tel:18019214488 FreeSWITCH Conference via PSTN]
```