# rtp_secure_media_suites 

由Ryan Harris于2018年2月7日创建

## rtp_secure_media_suites

string 一个备用的、可选的变量，用于指定逗号分隔的可用加密套件列表。如果指定了此变量，则rtp\_secure\_media只需指定权限标志，而不需要指定套件列表。

语法：`rtp_secure_media_suites=<encryption suites列表>`