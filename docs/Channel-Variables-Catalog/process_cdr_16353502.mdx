# process_cdr

由Ryan Harris创建，最新修改日期是2018年02月08日

## process_cdr

字符串，表示如何处理通话详单记录。

选项：

* `false` \- 不处理记录
* `true` \- 处理a leg和b leg的记录。这是默认选项。
* `a_only` \- 只处理a leg的记录
* `b_only` \- 只处理b leg的记录

此变量无条件导出。

**用法**

```xml
<action application="set" data="process_cdr=a_only"/>
```