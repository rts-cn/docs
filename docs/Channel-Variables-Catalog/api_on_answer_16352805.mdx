# api_on_answer

由Ryan Harris创建，上次修改日期为2018.02.07

## api_on_answer

当被叫方接听时执行一个API（而不是一个应用程序）。要设置一个应用程序，请使用[execute\_on\_answer](../FreeSWITCH-Explained/Dialplan/Variables-Master-List_16352570.mdx#execute_on_answer)。

**使用方法**

```xml
<action application="export" data="nolocal:api_on_answer=uuid_broadcast ${uuid} beep.wav both"/>
```

```xml
<action application="bridge" data="{api_on_answer='uuid_broadcast ${uuid} beep.wav both'}sofia/gateway/provider/5551231234"/>
```