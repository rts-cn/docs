# record_post_process_exec_api 

由 Ryan Harris 创建，最后修改于 2018.02.07

## record_post_process_exec_api

string 执行一个用于后处理记录音频的API。这两个变量允许对记录的音频进行后处理。之所以需要这样做是因为，如果通话中A路先挂断，拨号计划停止被处理，然后您无法对录制的文件采取任何操作。

**用法**

```xml
<action application="set" data="record_post_process_exec_api=api_here:api_arg1 api_arg2 api_arg3" />
```