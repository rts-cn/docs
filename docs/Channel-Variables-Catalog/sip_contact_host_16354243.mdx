# sip_contact_host

由 Ryan Harris 创建，最后修改于 2018.02.08

## sip_contact_host

字符串，表示`Contact` SIP 头部的主机名部分。

**用法**

如果您的请求头部包含：

```xml
Contact: <sip:gw+test@server.example.com:5060;transport=udp;gw=test>
```

那么该字段的值将是：

```xml
server.example.com
```