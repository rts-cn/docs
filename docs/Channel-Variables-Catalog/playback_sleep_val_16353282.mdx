# playback_sleep_val

由Ryan Harris创建，上次修改时间为2018年2月7日

## playback_sleep_val

整型，播放文件后暂停的时间。默认值为250毫秒。

**用法**

要连续播放一系列短文件，不间断地进行播放：

```xml
<action application="set" data="playback_sleep_val=0"/>
```