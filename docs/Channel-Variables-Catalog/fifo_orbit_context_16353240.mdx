# fifo_orbit_context

由Ryan Harris创建，上次修改时间为2018.02.07

## fifo_orbit_context

字符串 设置[fifo_orbit_exten](../FreeSWITCH-Explained/Dialplan/Variables-Master-List_16352570.mdx#fifo_orbit_exten)的上下文。必须在将呼叫者放入队列之前设置。

**用法**

```xml
<action application="set" data="fifo_orbit_context=MyContext"/>
```