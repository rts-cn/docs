# record_restart_limit_on_dtmf

由 Ryan Harris 创建，最后修改于 2018.02.07

## record_restart_limit_on_dtmf

布尔类型。当设置为 true 时，允许录音者按下 DTMF 键并延长录音超时时间。

**使用方法**

```xml
<action application="set" data="record_restart_limit_on_dtmf=true"/>
```