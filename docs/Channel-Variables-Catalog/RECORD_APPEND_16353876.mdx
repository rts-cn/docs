# RECORD_APPEND

创建者：Ryan Harris，上次修改日期：2018.02.08

## RECORD_APPEND

布尔类型Recording被追加到文件中。在通道上设置`RECORD_APPEND=true`，则所有支持此功能的录音格式（如WAV等）将以这种方式进行。

**用法**

```xml
<action application="set" data="RECORD_APPEND=true"/>
```