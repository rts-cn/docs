# rtp_adv_audio_ip 

由 Ryan Harris 创建，最后修改时间为 2018.02.08

## rtp_adv_audio_ip

string 这个通道变量是为了在一次性地设置 SDP 中的 IP 地址。正确的做法是在 sofia 配置文件中使用 `ext-rtp-ip`。

**用法**

```xml
<action application="set" data="rtp_adv_audio_ip=127.0.0.1" />
```