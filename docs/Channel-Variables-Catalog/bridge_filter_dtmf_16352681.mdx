# 桥接过滤DTMF

创建人：Ryan Harris，最新修改日期2018.02.07

## 桥接过滤DTMF

boolean 将此变量设置为`true`，将阻止从桥接的通道接收的DTMF数字发送到其他通道。

**用法**

在拨号计划中：

```xml
<action application="set" data="bridge_filter_dtmf=true" />
```

拨号字符串：

```xml
{bridge_filter_dtmf=true}sofia/default/blah@baz.com
```