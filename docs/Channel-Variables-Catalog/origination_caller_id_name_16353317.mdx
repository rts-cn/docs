# origination_caller_id_name

由Ryan Harris创建，最后修改于2018.02.07

## origination_caller_id_name

string 设置起始呼叫方的呼叫者ID名称（LEG A）。

如果你想在起始呼叫上设置呼叫者ID，你应该将其添加到拨号字符串之前的 `{}` 括号内。

**使用方法**

```xml
<action application="set" data="origination_caller_id_name=Uncle Sam"/>
```