# sip_ignore_reinvites

由Ryan Harris创建，上次修改日期为2018.02.07

## sip_ignore_reinvites

boolean 告诉FreeSWITCH要接受/忽略来自远程端的重INVITE。

**用法**

在桥接后不允许任何重INVITE：

```xml
<action application="set" data="sip_ignore_reinvites=true"/>
```