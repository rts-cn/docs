# originate_continue_on_timeout

由Ryan Harris创建，在2018.02.07最后修改

## originate_continue_on_timeout

布尔值，用于控制桥接是否在超时后继续。此变量在每个 `|` 后重置超时时间。默认值为 `false`：在首次超时后结束。

**用法**

```xml
<action application="set" data="originate_continue_on_timeout=true"/>
```