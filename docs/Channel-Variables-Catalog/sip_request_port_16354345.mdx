# sip_request_port

由 Ryan Harris 创建，最近修改日期为 2018.02.08

## sip_request_port

SIP 头部的端口部分。

**用法**

如果您的请求头部如下：

```xml
REGISTER sip:172.16.44.8:5061 SIP/2.0
```

那么此字段的值将为：

```xml
5061
```

然而，如果您的请求头部不包含端口，则此字段的值将为默认的 SIP 端口（通常为 5060）：

```xml
REGISTER sip:172.16.44.8 SIP/2.0
```