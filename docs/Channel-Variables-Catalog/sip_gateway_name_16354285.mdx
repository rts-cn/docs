# sip_gateway_name

由 Ryan Harris 创建，上次修改日期为 2018.02.08

## sip_gateway_name

字符串 如果您的呼叫是出站呼叫，并且 Leg B 经由网关路由，则此变量将包含网关名称。

**用法**

从内部配置文件中的用户呼叫，通过网关 "ntl" 路由出去：

```xml
sip_profile_name=gateway
sip_gateway_name=ntl
sofia_profile_name=internal
```