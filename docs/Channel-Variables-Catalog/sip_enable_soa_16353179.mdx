# sip_enable_soa 

 创建者：Ryan Harris，上次修改时间：2018.02.07

## sip_enable_soa

布尔型，用于每个呼叫的基础，可以设置为 `false`，以禁用SIP SOA从sofia，并很可能导致SDP的不受影响的交换。

**使用方法**

```xml
<action application="set" data="bypass_media=true"/>
<action application="export" data="sip_enable_soa=false"/>
```