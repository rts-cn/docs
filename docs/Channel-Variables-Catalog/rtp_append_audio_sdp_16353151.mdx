# rtp_append_audio_sdp

由Ryan Harris创建，最后修改于2018年02月07日

## rtp_append_audio_sdp

字符串 这可以用于将音频参数附加到发送到B-leg的SDP中。

在桥接之前必须设置它。

**用法**

```xml
<action application="export" data="rtp_append_audio_sdp=a=fmtp:18 annexb=no"/>
```