# sip_require_timer

由Ryan Harris创建，上次修改时间为2018.02.08

## sip_require_timer

在默认情况下，FS（FreeSWITCH）在会话刷新时需要计时器，除非是T.38重邀请。要禁用会话刷新时的计时器，请将其设置为`false`。

**用法**

```xml
<action application="set" data="sip_require_timer=false"/>
```