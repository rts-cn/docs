# bridge_pre_execute_aleg_app 

由 Ryan Harris 创建，最近修改日期为2018.02.07。

## bridge_pre_execute_aleg_app

在两个通道连接之前，在A leg上执行的命令或API字符串。可以通过设置`bridge_pre_execute_aleg_data`来传递应用程序参数。

![(warning)](/images/icons/emoticons/warning.svg) 此操作在呼叫设置完成后但媒体（音频）连接之前执行。