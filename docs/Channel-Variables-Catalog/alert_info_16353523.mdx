# alert_info

由Ryan Harris创建，最后修改日期为2018.02.08

## alert_info

string 添加一个`Alert-Info`头，像Snom和其他电话一样需要响铃或铃声信息。

**用法**

在所有呼叫上加载外部WAV铃声：

```xml
<action application="export" data="alert_info=http://192.168.181.5/sounds/ctu.wav"/>
```

使用外部铃声（在Snom的设置 > 首选项 > Alert-Info Ringer下更改alert-external、alert-group和alert-internal的铃声）：

```xml
<action application="export" data="alert_info=http://www.notused.com;info=alert-external;x-line-id=0"/>
```