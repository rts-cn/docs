# sip_jitter_buffer_plc

由 Ryan Harris 创建，最后修改于 2018.02.08

## sip_jitter_buffer_plc

通过启用/禁用网络抖动缓冲器 (jitter buffer) 时启用/禁用数据包丢失掩盖 (PLC)。在启用网络抖动缓冲器时，默认启用PLC。在启用网络抖动缓冲器之前设置此变量，才能使其生效。

**用法**

```xml
<action application="set" data="sip_jitter_buffer_plc=true"/>
```

```xml
<action application="set" data="sip_jitter_buffer_plc=false"/>
```