# 只截取未接听的呼叫

由Ryan Harris创建，最后修改日期为2018.02.07

## intercept_unanswered_only

boolean类型 如果设置为`true`，则只有在通道未接听时才会截取该线路。

**默认值：** `false`

**用法**

```xml
<action application="set" data="intercept_unanswered_only=true"/>
<action application="intercept" data="myUUID"/>
```