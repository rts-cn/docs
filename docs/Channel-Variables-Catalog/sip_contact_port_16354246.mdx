# sip_contact_port

由Ryan Harris创建，上次修改日期为2018.02.08

## sip_contact_port

integer类型，表示`Contact` SIP头部的端口部分。

**用法**

如果您的请求头部包含：

```xml
Contact: <sip:gw+test@server.example.com:5060;transport=udp;gw=test>
```

那么此字段的值应为：

```xml
5060
```