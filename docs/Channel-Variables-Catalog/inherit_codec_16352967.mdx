# inherit_codec

创建者：Ryan Harris，最后修改时间：2018.02.07

## inherit_codec

boolean 如果开启了延迟协商，并且您在A leg上设置了inherit\_codec=true，那么B leg的协商编解码器将被强制应用于A leg。

**用法**

```xml
<action application="set" data="inherit_codec=true"/>
```