# sip_auto_simplify 

由Ryan Harris创建，上次修改日期为2018.02.07 

## sip_auto_simplify

boolean 当设置为真时，这会指示FreeSWITCH在可以安全地这样做的情况下，从SIP信令路径中移除自身。

**用法**

```xml
<action application="set" data="sip_auto_simplify=true"/>
```