# campon_retries 

由Ryan Harris创建，最后修改于2018年02月09日

## campon_retries

整数值，控制在回退之前网桥重试的次数。

默认值: `100`

**用法**

```xml
<action application="set" data="campon=true"/>
<action application="set" data="campon_retries=13"/>
<action application="bridge" data="sofia/gateway/myprovider/5551231234"/>
```