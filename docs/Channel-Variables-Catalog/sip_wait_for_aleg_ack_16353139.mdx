# sip_wait_for_aleg_ack

由 Ryan Harris 创建，最后修改日期为2018.02.07

## sip_wait_for_aleg_ack

当您在出站呼叫的b leg中的`{}`中设置变量`sip_wait_for_aleg_ack`时，这将使b leg推迟发送`ACK`直到它看到A leg已经收到一个ack。

**用法**

```xml
<action application="bridge" data="{sip_wait_for_aleg_ack=true}sofia/internal/foo@bar.com"/>
```