# 录音出错时挂断

由Ryan Harris创建，最后修改于2018.02.07

## 录音出错时挂断

boolean 当设置为`true`时，这个通道变量将导致在尝试录制通话时出现错误时挂断通话。这不是一个常见的功能，但在必须录制通话的情况下，它使得不能有未录制的通话成为可能。（在某些商业场景中非常有用。）

**使用方法**

```xml
<action application="set" data="RECORD_HANGUP_ON_ERROR=true"/>
```