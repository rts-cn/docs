# disable_rtp_auto_adjust

由Ryan Harris创建，上次修改日期为2018年02月07日

## disable_rtp_auto_adjust

字符串 如果RTP自动调整行为不符合您的期望，可以禁用它。这将阻止交换机根据源地址重写RTP目标地址。当RTP Auto-Adjust打开时，FreeSWITCH将更改目标RTP地址以与入站数据包的源匹配，但如果另一端的确希望在不同的地址上发送和接收，这种方法就不起作用。

**用法**

```xml
在拨号字符串中添加{disable_rtp_auto_adjust=true}。
```