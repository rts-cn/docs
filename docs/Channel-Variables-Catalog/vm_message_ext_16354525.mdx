# vm_message_ext

由Ryan Harris创建，上次修改于2018年02月08日

## vm_message_ext

string 确定语音邮件消息的保存格式。也可以使用参数`vm-message-ext`来设置。

此变量在拨号计划中设置。

默认可用选项:

- `mp3`
- `wav`

**使用方法**

```xml
<action application="set" data="vm_message_ext=mp3"/>
```