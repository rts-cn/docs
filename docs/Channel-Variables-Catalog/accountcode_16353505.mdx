# 账户代码

由Ryan Harris创建，最后修改于2018年2月8日

## 账户代码

字符串账户代码通常是您可以按每个通话路线分配的任意值。账户代码的一个重要特性是，如果其值与`cdr_csv.conf.xml`中定义的CDR CSV模板之一匹配，那么在生成CSV CDR时将使用该CDR模板。

**用法**

```xml
<action application="set" data="accountcode=custom"/>
```