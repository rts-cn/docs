# caller_id_number 

由Ryan Harris创建，上次修改日期为2018.02.07

## caller_id_number

string readonly 入站呼叫设置的呼叫者ID电话号码，不是真实变量。实际上只能读取。来自sofia.c，使用的值（按优先级）为：`P-Preferred-Identity`、`P-Asserted-Identity`、`Remote-Party-ID`和`From`头部。