# conference_last_matching_digits

由Ryan Harris创建，最近修改日期为2018.02.07

## conference_last_matching_digits

dtmf digit 只读字段。包含用户在该通道中发送到会议的最后匹配的数字。

**使用方法**

```xml
<action application="log" data="INFO 最后几位数字的发送者：${conference_last_matching_digits}"/>
```