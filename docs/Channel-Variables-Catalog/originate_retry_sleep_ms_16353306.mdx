# 发起重试睡眠时间（毫秒）

由Ryan Harris创建，于2018年2月7日修改

## 发起重试睡眠时间（毫秒）

整数型，用于设置FreeSWITCH在发送邀请消息给接收网关之间的等待时间。

**用法**

在发送邀请消息给被叫网关之间等待500毫秒：

```xml
<action application="set" data="originate_retry_sleep_ms=500"/>
```