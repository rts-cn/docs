# media_webrtc 

由Ryan Harris创建，最后修改于2018年2月8日

## media_webrtc

boolean用于指示FS生成WebRTC呼叫的INVITE。例如，如果您需要发起一个WebRTC呼叫，但您并没有呼叫一个在FS上注册的SIP UA（如果UA已经在FS上注册，FS知道它应该发起一个WebRTC呼叫）。

**用法**

{media\_webrtc=true}