# playback_timeout_sec 

由 Ryan Harris 创建，最后修改日期为 2018.02.07

## playback_timeout_sec

整数 设置播放超时时间。如果您想播放一段可能非常长的文件的简短摘录，这非常有用。

**用法**

```xml
<action application="set" data="playback_timeout_sec=10"/>
```