# verbose_sdp 

由Ryan Harris创建，最近修改于2018年02月07日

## verbose_sdp

boolean RFC要求为IANA动态载荷类型提供一个rtpmap。对于已知的静态载荷（如PCMU、PCMA、G729等），不需要有带有编解码器名称和载荷类型的rtpmap。

默认情况下，FreeSWITCH将`verbose_sdp=false`，不包含静态载荷类型的rtpmap。如果您的设备不支持这个（可耻的设备），请设置`verbose_sdp=true`，它将包括任何静态载荷类型的rtpmap（以前的默认行为）。