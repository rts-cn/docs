# sip_to_host

由Ryan Harris创建，于2018.02.08最后修改

## sip_to_host

`To` SIP标头中的主机名部分。

**用法**

如果您的请求标头包含：

```xml
To: <sip:1000@server.example.com>;tag=0e70ba56
```

那么该字段的值将为：

```xml
server.example.com
```