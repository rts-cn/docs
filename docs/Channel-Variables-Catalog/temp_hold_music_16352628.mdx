# temp_hold_music

由Ryan Harris创建，最近修改日期为2018.02.07

## temp_hold_music

string 这个变量指定了一个保持音乐的值，只会在呼叫者被转接之前播放。转接后，`hold_music`变量会生效。

**用法**

```xml
<action application="set" data="temp_hold_music=local_stream://alternate_moh"/>
```