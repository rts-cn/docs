# sip_contact_user

由Ryan Harris创建，最近修改日期为2018.02.08

## sip_contact_user

`Contact` SIP头部的用户名部分。

**用法**

如果您的请求头包含：

```xml
Contact: <sip:gw+test@server.example.com:5060;transport=udp;gw=test>
```

那么该字段的值将为：

```xml
gw+test
```