# sip_cid_in_1xx

由Ryan Harris创建，上次修改于2018.02.08

## sip_cid_in_1xx

boolean类型。阻止FreeSWITCH在从leg-B接收到183响应后自动在发送给leg-A之前插入RPID。可以在配置文件中定义全局属性：`pass-callee-id=false`。

**用法**

```xml
<action application="set" data="sip_cid_in_1xx=false"/>
```